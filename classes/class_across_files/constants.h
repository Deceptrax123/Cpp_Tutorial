#ifndef CONSTANTS_H /*Prevents duplicate definition of components in headerfiles.            \
                    If the header file is not defined, the components in it will be defined. \
                    Else it will be ignored as it has already been included.*/
#define CONSTANTS_H

double const pi = 3.14;

#endif